# === Telegram API (один app на my.telegram.org, общий для всех аккаунтов) ===
TG_API_ID=
TG_API_HASH=

# Прокси: если false, поле proxy у аккаунтов игнорируется, клиенты идут
# напрямую с IP сервера. Для теста удобно; на проде с пулом 50+ аккаунтов
# поднимай в true и заполняй proxy в accounts.csv.
PROXIES_ENABLED=false

# === LLM ===
ANTHROPIC_API_KEY=
USE_LOCAL_MODEL=false
LOCAL_MODEL_URL=http://localhost:8000/v1

# === Дефолты для импорта новых аккаунтов ===
# (per-account лимиты в CSV/таблице accounts перебивают эти значения)
DEFAULT_DAILY_LIMIT=50
DEFAULT_HOURLY_LIMIT=8
WEEKEND_MULTIPLIER=0.5

# === Паузы между отправками (глобально) ===
MIN_DELAY_SECONDS=420
MAX_DELAY_SECONDS=3600

# === Окно активности ===
WORKING_HOURS_START=10
WORKING_HOURS_END=22
MANDATORY_SLEEP_START=0
MANDATORY_SLEEP_END=8

# === Имитация поведения ===
SIMULATE_TYPING=true
TYPING_SECONDS_PER_CHAR=0.08
TYPING_MAX_SECONDS=45
READ_INCOMING_BEFORE_SEND=true
SKIP_PROBABILITY=0.15

# === Антиспам-фильтры ===
MIN_INCOMING_MESSAGES=3
MIN_DAYS_BETWEEN_SENDS=30
REPLY_RATE_WINDOW=15
REPLY_RATE_MIN=0.25
REPLY_RATE_CHECK_HOURS=48

# === Ramp-up (применяется к каждому аккаунту индивидуально) ===
RAMPUP_ENABLED=true
RAMPUP_DAYS=14
RAMPUP_START_LIMIT=3

# === Хранилище ===
DB_PATH=data/tg_assistant.db
